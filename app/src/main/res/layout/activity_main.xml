<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0d0d12">

    <com.example.rubikscube.CubeGLSurfaceView
        android:id="@+id/glView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Barre supérieure -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="14dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/app_name"
            android:textColor="#EEEEEE"
            android:textSize="16sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnLevel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎚 Niveau"
            android:textAllCaps="false"
            android:textSize="13sp" />

        <Button
            android:id="@+id/btnTheme"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:text="🎨 Thème"
            android:textAllCaps="false"
            android:textSize="13sp" />
    </LinearLayout>

    <!-- Chrono + coups -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center_horizontal"
        android:layout_marginTop="70dp"
        android:orientation="horizontal"
        android:background="#66000000"
        android:padding="8dp">

        <TextView
            android:id="@+id/txtTimer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0.0 s"
            android:textColor="#4DD0E1"
            android:textSize="18sp"
            android:textStyle="bold"
            android:minWidth="90dp"
            android:gravity="center" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="  •  "
            android:textColor="#88FFFFFF"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/txtMoves"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 coups"
            android:textColor="#FFD54F"
            android:textSize="18sp"
            android:textStyle="bold"
            android:minWidth="90dp"
            android:gravity="center" />
    </LinearLayout>

    <!-- Joystick de rotation (coin bas gauche) -->
    <com.example.rubikscube.JoystickView
        android:id="@+id/joystick"
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="20dp"
        android:layout_marginBottom="26dp" />

    <!-- Actions (coin bas droit) -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:orientation="vertical"
        android:padding="16dp">

        <Button
            android:id="@+id/btnScramble"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:text="🔀 Mélanger"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btnReset"
            android:layout_width="140dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="↺ Réinitialiser"
            android:textAllCaps="false" />
    </LinearLayout>

    <!-- Astuce -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="150dp"
        android:text="Glisse sur le cube pour tourner une face • joystick pour pivoter"
        android:textColor="#88FFFFFF"
        android:textSize="11sp"
        android:gravity="center" />
</FrameLayout>
